这是一个离散数学教学web应用，项目结构是vue + spring boot，我采用nignx + spring boot将其部署在虚拟机上（非本机环境，本机只有相关代码文件），部署流程和涉及的文件见'/home/admin-unix/deedm_alpha/部署流程.md'。教学网站的特点是直接的教学展示，没有账户登录，产生的数据只是示例图片，h2是文件模式，把产生的数据放在一个文件夹。后端程序默认把生成的图片放到./data。然后点击屏幕上的“清空”按钮会清空文件夹，潜在问题是，如果当前访问节点清空了内容，另一个访问节点对应的内容缺源。更标准的做法确实是不同账户+数据库，但是现在马上要交付了，要进行安全扫描。数据库-安全审计-审计范围应覆盖到每个数据库用户这一项，我有一个方案，见'/home/admin-unix/deedm_alpha/aop.md'。评估方案优劣，完善方案（直接修改），然后执行


这是一个离散数学教学web应用，项目结构是vue + spring boot，我采用nignx + spring boot将其部署在虚拟机上（非本机环境，本机只有相关代码文件），部署流程和涉及的文件见'/home/admin-unix/deedm_alpha/部署流程.md'。教学网站的特点是直接的教学展示，没有账户登录，产生的数据只是示例图片，h2是文件模式，把产生的数据放在一个文件夹。后端程序默认把生成的图片放到./data。现在进行安全扫描：数据库-安全审计-审计内容应包括重要用户行为、系统资源的异常使用和重要系统命令的使用等系统内重要的安全相关事件。同时，spring服务输出的调试信息太多了，主要集中在controller层。在扫描这些调试信息的同时，你可以判断是否是安全相关的事件，决定是否删掉。你先指定审计添加内容和调试信息删除计划


这是一个离散数学教学web应用，项目结构是vue + spring boot，我采用nignx + spring boot将其部署在虚拟机上（非本机环境，本机只有相关代码文件），部署流程和涉及的文件见'/home/admin-unix/deedm_alpha/部署流程.md'。教学网站的特点是直接的教学展示，没有账户登录，产生的数据只是示例图片，h2是文件模式，把产生的数据放在一个文件夹。用户输入仅限于文本，无文件上传。文本类型包括：latex算式，数字,字母。现在进行安全扫描项：应用安全-软件容错-应提供数据有效性检验功能，保证通过人机接口输入或通过通信接口输入的数据格式或长度符合系统设定要求；你给出检查方案